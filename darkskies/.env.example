# Railway sets DATABASE_URL automatically when you add a Postgres plugin.
# Locally you can leave this unset — the app falls back to a data.json file.
# DATABASE_URL=postgres://user:pass@host:5432/dbname
# PORT=3000
